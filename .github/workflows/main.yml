name: CI Build

on:
  push:
    branches:
      - dev
      - dev-automated-build
      - main
      - actions
      - make-tweaks

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/actions/setup
      - uses: ./.github/actions/build
