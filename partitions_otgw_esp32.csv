# OTGW-firmware ESP32-S3 partition table — single app (no OTA), 4MB flash
# Layout mirrors the ESP8266 4M2M scheme: a generously sized filesystem so
# the web UI assets (fonts, ECharts custom bundle, JS/CSS) can grow without
# nibbling at the firmware budget. To enable proper OTA (two app slots) a
# board with 8MB flash is required.
#
# 4MB flash layout:
#   1.875 MB app (ota_0) + 2.0 MB LittleFS (label "spiffs") + 64 KB coredump
#
# Bootloader: 0x0 (ESP32-S3 specific — differs from original ESP32 which uses 0x1000)
# Name,   Type, SubType, Offset,  Size,    Flags
nvs,      data, nvs,     0x9000,  0x5000,
otadata,  data, ota,     0xE000,  0x2000,
app0,     app,  ota_0,   0x10000, 0x1E0000,
spiffs,   data, spiffs,  0x1F0000,0x200000,
coredump, data, coredump,0x3F0000,0x10000,
