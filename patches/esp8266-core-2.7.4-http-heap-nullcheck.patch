--- a/libraries/ESP8266WiFi/src/include/DataSource.h	2026-06-08 21:45:03.686394100 +0200
+++ b/libraries/ESP8266WiFi/src/include/DataSource.h	2026-06-08 21:45:04.160575300 +0200
@@ -76,6 +76,7 @@
         //Buffer too small?
         if (_bufferSize < min_buffer_size) {
             uint8_t *new_buffer = new uint8_t[min_buffer_size];
+            if (!new_buffer) return nullptr;   // OTGW(TASK-844): OOM under fragmentation - bail instead of readBytes/memcpy into NULL (StoreProhibited, ROM memcpy 0x4000df64)
             //If stream reading is ahead, than some data is already in the old buffer and needs to be copied to new resized buffer
             if (_buffer && stream_read > 0) {
                 memcpy(new_buffer, _buffer.get(), stream_read);
--- a/libraries/ESP8266WiFi/src/include/ClientContext.h	2026-06-08 21:45:03.876215800 +0200
+++ b/libraries/ESP8266WiFi/src/include/ClientContext.h	2026-06-08 21:45:04.162996500 +0200
@@ -506,6 +506,7 @@
             if (!next_chunk_size)
                 break;
             const uint8_t* buf = _datasource->get_buffer(next_chunk_size);
+            if (!buf) break;   // OTGW(TASK-844): get_buffer OOM (NULL) - come back later instead of tcp_write(NULL)
 
             uint8_t flags = 0;
             if (next_chunk_size < _datasource->available())
