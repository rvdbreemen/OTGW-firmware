# Placeholder — real file is built on the dev host.
#
# Run the build steps in PATCH.md (Step 1) and overwrite this file with
# the resulting `dist/echarts.min.js` from the ECharts repo.
#
# Expected size: ~180 KB minified, ~50 KB gzipped.
# Pinned version: 5.4.3 (see echarts.VERSION).
